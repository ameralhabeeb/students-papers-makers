{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "User profile containing their progress and notes",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string"
        },
        "notes": {
          "type": "string",
          "description": "User notes"
        },
        "completedSteps": {
          "type": "object",
          "description": "Completed steps for each article type"
        },
        "updatedAt": {
          "type": "string",
          "format": "timestamp"
        }
      },
      "required": ["userId", "updatedAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/UserProfile" },
      "description": "Stores the user's progress and notes."
    }
  }
}
